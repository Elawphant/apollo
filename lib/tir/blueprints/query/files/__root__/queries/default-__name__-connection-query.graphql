query <%= classifiedModuleName %>ConnectionQuery <%= connection.queryParamVars %> {
    <%= connection.alias %>: <%= connection.connectionRoot %> <%= connection.queryParams %> {
        <%= connection.queryFields %>
    }
}